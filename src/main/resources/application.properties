# Remote Database Config (s72510_app)
# spring.datasource.url=jdbc:mysql://localhost:3306/s72510_app?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
# spring.datasource.username=s72510_app
# spring.datasource.password=Luqman.1

# Local Database Config (football_db)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

server.port=${PORT:8080}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=60000

# 2. JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 3. Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.enabled=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.web.resources.cache.period=0
spring.web.resources.chain.cache=false




# 4. File Upload Directory
# Override via environment variable UPLOAD_DIR in production (e.g. /data/uploads on Railway)
upload.dir=${UPLOAD_DIR:uploads}

# Gemini AI API Key
gemini.api.key=AIzaSyATamWFVHrr-hWmjrcboPPbLh0Sr7RcUSo
# EMAIL CONFIGURATION (Gmail)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:luqmakim71@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:hnbh hqyo brsh epit}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ToyyibPay Payment Gateway (Sandbox - dev.toyyibpay.com)
toyyibpay.secret-key=${TOYYIBPAY_SECRET_KEY:iia8sv1v-eul5-xr1g-j4c6-ea0fn6p8rndx}
toyyibpay.category-code=${TOYYIBPAY_CATEGORY_CODE:0rnz2fzx}
toyyibpay.base-url=${TOYYIBPAY_BASE_URL:https://dev.toyyibpay.com}
toyyibpay.app-url=${APP_URL:https://s72510.up.railway.app}